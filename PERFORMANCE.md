# 性能优化与验证

## 行为变化

- 查询预览最多保留 1000 行；检测到第 1001 行时取消未读结果，并在状态栏提示截断。全量导出不受此限制。没有重写用户 SQL，也没有添加服务端分页；排序、聚合等数据库执行开销仍取决于查询本身。预览耗时包含数据绑定调用。
- 两个多表 Excel 导出入口统一使用 MiniExcel。每个 Sheet 的 Reader 延迟打开，在读完该 Sheet 时释放 Reader、Command 和数据库连接。空表也保留表头。
- SQLite 的 Reader、DataTable、指定列映射导入共用参数化写入，每张表复用命令。一次导入使用一个事务，失败时整批回滚；不会先把 Reader 转成 DataTable。
- SQL Server 到 SQLite 的迁移按表缓存参数和目标类型；保留每 1000 行提交的原有语义。因此取消或失败后，之前已提交的批次仍保留。
- SQL Server 元数据按对象 ID 建立查找表，避免逐表扫描完整列和外键集合；SQLite 一次读取列元数据后按表分组。
- 窗体中的后台数据库操作使用独立访问对象，同一时间只运行一个操作。任务期间禁用输入和操作按钮，迁移停止、进度按钮仍可使用。
- 移除业务流程中的强制 GC；Reader 负责连带释放其 Command，并按事务及 `IsKeepConnect` 设置关闭连接。

## 回归测试

在仓库目录运行：

```powershell
dotnet build DataPieDesktop.sln
dotnet run --project DataPieCore.RegressionTests/DataPieCore.RegressionTests.csproj
```

测试不依赖外部数据库，使用独立的临时 SQLite 文件和 XLSX/CSV 文件，并在结束后清理本次测试目录。覆盖：

- 文本、数字、二进制、NULL、特殊标识符与列映射导入；约束失败时事务回滚。
- 10 万行导入、1000 行预览上限、恰好达到上限、重复列名。
- Reader 正常关闭、保留连接和 SQL 执行失败时的连接状态。
- CSV/XLSX 导入，以及两个多表导出入口。
- 120 个 Sheet 的名称、顺序、内容、空表表头；通过访问对象代理断言源连接峰值不超过 1。
- 导出中途失败时释放连接；批量 SQLite Schema 读取。

测试输出的导入耗时仅反映本次机器及测试数据，不是优化前后对照基准。

## 仍需实库验证

SQL Server 的连接生命周期、元数据读取、跨数据库迁移和 WinForms 交互仍需实际环境验证。建议用包含宽表、大字段、不同 schema 下同名表的数据库检查迁移结果，并测试中途停止后再次运行。

构建可能报告已有的过时 API 和 NuGet 依赖安全警告。本次没有升级依赖包。
